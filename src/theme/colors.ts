import { orange } from "@mui/material/colors";

const colors = {
  white: "#ffffff",
};

export const primary = {
  light: orange[300],
  main: orange[700],
  dark: orange[800],
  contrastText: colors.white,
};
